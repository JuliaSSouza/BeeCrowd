#include <stdio.h>
#include <string.h>

int main(){

int N;
scanf("%d", &N);

int K[N];
char st[100];

for(i = 0; i < N; ++i)
scanf("%d", &K[i]);

  for(i = 0; i < K[N]; ++i)
  scanf("%s", st[i]);


if(strcomp(st[i], st[i + 1]))
    return 0;
}